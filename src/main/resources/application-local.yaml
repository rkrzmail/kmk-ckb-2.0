env: local
spring:
  boot:
    admin:
      client:
        url: http://localhost:9000
        instance:
          name: Kmk-Digital-2.0
        credentials:
          username: vandikalvandi@gmail.com
          password: 12345
  datasource:
    url: jdbc:postgresql://aurora-kmk.cluster-cc6z1g8cfqcv.ap-southeast-3.rds.amazonaws.com:5432/kmkds?stringtype=unspecified&connectTimeout=10&socketTimeout=30&tcpKeepAlive=true
    username: postgres
    password: Csultmt10
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 30000
      validation-timeout: 5000
      idle-timeout: 300000
      max-lifetime: 1200000
      keepalive-time: 300000
      initialization-fail-timeout: 0
      connection-test-query: SELECT 1
  data:
    mongodb:
      host: ${MONGO_HOST:localhost}
      port: ${MONGO_PORT:27017}
      database: ${MONGO_DATABASE:kmklog}
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      database: ${REDIS_DATABASE:6}
      password: ${REDIS_PASSWORD:}
      timeout: ${REDIS_TIMEOUT:6000}
      lettuce:
        pool:
          enabled: true
          max-active: ${REDIS_POOL_MAX_ACTIVE:30}
          max-idle: ${REDIS_POOL_MAX_IDLE:10}
          min-idle: ${REDIS_POOL_MIN_IDLE:2}
          shutdown-timeout: ${REDIS_POOL_SHUTDOWN_TIMEOUT:1000}
          max-wait: ${REDIS_POOL_MAX_WAIT:10000}

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    locations: classpath:db/migration/dev/{vendor}
    schemas: public
    enabled: true
    baseline-on-migrate: true
    out-of-order: true

  cache:
    type: redis
    redis:
      time-to-live: 60000

  mail:
    host: srv150.niagahoster.com
    port: 465
    protocol: smtp
    username: csul@nikita.co.id
    password: DArS!OP#csul6?6Uu
    properties:
      mail.smtp.ssl.trust: '*'
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

# region testing mail env
testing:
  mail:
    host: srv150.niagahoster.com
    port: 465
    protocol: smtp
    username: csul@nikita.co.id
    password: DArS!OP#csul6?6Uu

# Keep your custom collection property anywhere you like
custom:
  mongodb:
    collection: event

jwt:
  env:
    security:
      jwt:
        secret-key: 98d8710219d8224094b7fcb956c054f15d633ad1dfd18d42fdd7f59d77d69f69
        expiration-time: 1800000

spring-doc:
  version: '3.0'
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  file: ./logs/log-file.log
  level:
    org.hibernate: ERROR
    org.springframework.web: ERROR
    root: INFO
    feign: DEBUG                             # Enables basic Feign engine debug logs
    org.springframework.cloud.openfeign: DEBUG # Enables Spring integration logs

server:
  error:
    whitelabel:
      enabled: false
  port: 8080
rsa:
  private-key: classpath:certs/pkcs8.pem
  public-key: classpath:certs/public-key.pem


csul:
  sisca:
    v1:
      internal: http://10.30.15.146:8185/SISCAConnect/api/v1
      white_list: http://10.30.15.146:8185/SISCAConnect/api/v1
      auth:
        username: KMKDigital
        password: CSUljkt#2024
    mail-position:
      branch-admin: "ADM"
      account-officer: "AO/AM"
      branch-manager: "BM/BOH"
      area-sales-manager: "RM"
  mst:
    v1: http://goakcwwrshxfwkgm6bwnegpeti0ajaus.lambda-url.ap-southeast-1.on.aws/v1
  confins:
    adinskey: VlBJZllVQTZyUk5acW9FQVg3Yk9oSTlLT1lsRWUzMkllU1ZlZVZtaTExZ1NZUz0=
    fou:
      v1: http://uat-confinsr3.csulfinance.com/api/fou/v1
    mou:
      v1: http://uat-confinsr3.csulfinance.com/api/mou/v1
    #LOS
    los:
      v1: http://uat-confinsr3.csulfinance.com/api/los/v1
    #CORELOS
    corelos:
      v1: http://uat-confinsr3.csulfinance.com/api/corelos/v1
    ar:
      v1: http://uat-confinsr3.csulfinance.com/api/ar/v1
    payment:
      v1: http://uat-confinsr3.csulfinance.com/api/payment/v1
    ap:
      v1: http://uat-confinsr3.csulfinance.com/api/ap/v1
  ckb :
    url:
    api-key:
aspect:
  enabled: true

app:
  api:
    key: YiByHB@CSUL_DEV

# Feign Configuration
feign:
  csul:
    ckb:
      url: https://api-ckbvendorportal.dipstrategy.co.id
      username: ramco
      password: RamcoVDC2026!
      client-secret: SecretRamco2026!

  confins:
    url: https://uat-confinsr3.csulfinance.com
    adins-key: VlBJZllVQTZyUk5acW9FQVg3Yk9oSTlLT1lsRWUzMkllU1ZlZVZtaTExZ1NZUz0=